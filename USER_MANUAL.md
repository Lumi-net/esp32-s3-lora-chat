# ELC (ESP32-S3 LoRa Chat) 用户使用手册

> **Lumi-net** 出品 | 开源 LoRa 点对点通信设备

---

## 目录

- [ELC (ESP32-S3 LoRa Chat) 用户使用手册](#elc-esp32-s3-lora-chat-用户使用手册)
  - [目录](#目录)
  - [1. 快速上手](#1-快速上手)
    - [1.1 前提条件](#11-前提条件)
    - [1.2 开机与启动](#12-开机与启动)
    - [1.3 认识界面](#13-认识界面)
    - [1.4 发送第一条消息](#14-发送第一条消息)
    - [1.5 接收消息](#15-接收消息)
  - [2. 按键操作](#2-按键操作)
    - [2.1 T9 多击输入法](#21-t9-多击输入法)
    - [2.2 锁定/导航模式](#22-锁定导航模式)
  - [3. 界面详解](#3-界面详解)
    - [3.1 状态栏](#31-状态栏)
    - [3.2 菜单页（联系人列表）](#32-菜单页联系人列表)
    - [3.3 聊天页](#33-聊天页)
    - [3.4 输入区域](#34-输入区域)
  - [4. 联系人管理](#4-联系人管理)
    - [4.1 添加联系人](#41-添加联系人)
    - [4.2 删除联系人](#42-删除联系人)
    - [4.3 重命名联系人](#43-重命名联系人)
    - [4.4 联系人 ID 说明](#44-联系人-id-说明)
  - [5. 发送与接收消息](#5-发送与接收消息)
    - [5.1 直发消息](#51-直发消息)
    - [5.2 广播消息（ALL）](#52-广播消息all)
    - [5.3 消息去重](#53-消息去重)
    - [5.4 聊天记录](#54-聊天记录)
  - [6. 设置](#6-设置)
    - [6.1 设置项总览](#61-设置项总览)
    - [6.2 修改联系人别名](#62-修改联系人别名)
    - [6.3 修改用户颜色](#63-修改用户颜色)
    - [6.4 修改界面颜色](#64-修改界面颜色)
    - [6.5 Wi-Fi 配置](#65-wi-fi-配置)
    - [6.6 时间设置](#66-时间设置)
    - [6.7 自动休眠时间](#67-自动休眠时间)
    - [6.8 调节亮度](#68-调节亮度)
    - [6.9 自定义状态栏标题](#69-自定义状态栏标题)
    - [6.10 添加/删除联系人](#610-添加删除联系人)
    - [6.11 清空聊天记录](#611-清空聊天记录)
    - [6.12 恢复出厂设置（清空 NVS）](#612-恢复出厂设置清空-nvs)
    - [6.13 心跳开关](#613-心跳开关)
  - [7. 省电与休眠](#7-省电与休眠)
    - [7.1 自动休眠](#71-自动休眠)
    - [7.2 唤醒方式](#72-唤醒方式)
    - [7.3 快速休眠机制](#73-快速休眠机制)
  - [8. 电池电量](#8-电池电量)
  - [9. 界面颜色自定义](#9-界面颜色自定义)
  - [附录 A：编译与烧录](#附录-a编译与烧录)
    - [环境搭建](#环境搭建)
    - [构建与烧录](#构建与烧录)
    - [关键配置](#关键配置)
  - [附录 B：硬件接线参考](#附录-b硬件接线参考)
    - [主要外设接线](#主要外设接线)
    - [LoRa 模块引脚](#lora-模块引脚)
    - [注意事项](#注意事项)
  - [附录 C：sim\_lora.py 模拟器使用](#附录-csim_lorapy-模拟器使用)
    - [安装依赖](#安装依赖)
    - [基本用法](#基本用法)
    - [交互命令](#交互命令)
    - [示例：测试完整通信流程](#示例测试完整通信流程)
    - [注意事项](#注意事项-1)

---

## 1. 快速上手

### 1.1 前提条件

在使用 ELC 之前，请确认以下事项：

1. **至少准备两台设备**（或一台设备 + 一台运行 `sim_lora.py` 的电脑），单台设备无法进行点对点通信。
2. **设备 ID 已预设**。每台设备有一个唯一的十六进制 ID（如 `0x01`、`0x05`），在固件中硬编码（`self_id` 变量）。如需修改，请重新编译固件。
3. **所有设备使用相同 LoRa 频段和组号**。出厂默认为 470 MHz、组号 `0x00`。如果修改了组号，所有设备必须一致。
4. **建议先设置时间**。消息时间戳依赖 RTC 时钟，未设置时间时消息将显示 `00:00`。

### 1.2 开机与启动

接通电源后，设备显示开机动画：

1. 屏幕中央出现旋转动画（Spinner），下方依次显示初始化状态：
   - `System Booting...`
   - `Initializing Keyboard...`
   - `Initializing UART...`
   - `Initializing Flash...`
   - `Initializing I2C...`
   - `Initializing NVS...`
   - `Reading NVS...`
   - `Restoring Flash Offset...`
   - `Updating Latest Message Time...`
   - `Done!`
2. 初始化完成后自动进入**菜单页**（联系人列表）。

整个启动过程约 3-5 秒。

### 1.3 认识界面

界面分为三个区域：

```
┌─────────────────────────────────┐
│ 10/25 14:30   Lumi-net   🟢 85% │  ← 状态栏（15px）
├─────────────────────────────────┤
│                                 │
│  > Alice        0x01            │
│    02/02 15:37    02/02 14:28   │  ← 在线时间(左) + 消息时间(右)
│                                 │
│    Bob          0x02            │
│    Maybe Offline  No messages   │
│                                 │
│    ...                          │  ← 中间列表区
│                                 │
├─────────────────────────────────┤
│ [S][?]                Please... │  ← 输入区域
│                          112    │
└─────────────────────────────────┘
```

- **状态栏**：左侧显示时间，中间显示标题，右侧显示 LoRa 状态指示灯和电池电量
- **中间列表**：菜单页显示联系人列表，聊天页显示消息记录
- **输入区域**：左侧显示 Shift/Lock 图标和剩余字符数，右侧为文本输入框

### 1.4 发送第一条消息

1. 在菜单页，使用**导航模式**上下选择联系人，按**确认键**进入聊天页。
2. 使用 T9 输入法输入消息（详见 [2.2 节](#22-t9-多击输入法)）。
3. 按 **Shift + CAND4**（即键盘上标有 "SEND" 的位置）发送消息。
4. 等待对方设备确认（ACK）。状态栏指示灯变为**紫色**（等待确认），收到 ACK 后变为**绿色**（空闲）。

### 1.5 接收消息

- 收到消息时，如果当前在聊天页且对象匹配，消息自动追加到列表底部。
- 如果不在聊天页，消息仍然保存到外部闪存（Flash），下次进入聊天页时可查看。

---

## 2. 按键操作

### 2.1 T9 多击输入法

每个键的第一行为小写模式，第二行为大写模式，第三行为导航模式。
```
┌─────┬─────┬─────┬─────┐
│  1abc  │  2def  │  3ghi   │   C1   │
│  !ABC  │  ?DEF  │  'GHI   │        │
│  MENU  │   UP   │SETTINGS │  HOME  │
├─────┼─────┼─────┼─────┤
│  4jkl  │  5mno  │  6pqr  │   C2    │
│  -JKL  │  *MNO  │  /PQR  │         │
│  LEFT  │CONFIRM │  RIGHT │   END   │
├─────┼─────┼─────┼─────┤
│  7stu  │  8vwx  │  9yz(  │   C3    │
│  +STU  │  &VWX  │  ␣YZ)  │         │
│  BACK  │  DOWN  │ DELETE │  PGUP   │
├─────┼─────┼─────┼─────┤
│  SHIFT │  0␣^=  │BACKSPACE│  C4    │
│  SHIFT │  .,:_  │INPUTLOCK│ SEND   │
│  SHIFT │        │BACKSPACE│PGDOWN  │
└─────┴─────┴─────┴─────┘
```

T9 输入法分为两步：**先选键，再选字**。

**第一步：按数字键选键**

按下一个数字键（如 `1`、`2`、`5` 等），屏幕输入区域的锁图标会显示对应的键号（0-9）。此时你已经选中了该键。

**第二步：按候选键选字**

使用C1-C4候选键，选择当前键中的第1-4个候选。

**输入示例**：输入 "Hi"

1. 按 **SHIFT** 键 → Shift 图标变实心（大写模式）
2. 按 `3` 键 → 锁图标显示 `3`（对应字符`'GHI`）
3. 按 **C3** 键 → 选择键 3 的第 3 个候选（Shift 模式），得到 `H`
4. Shift 自动关闭
5. 按 `3` 键 → 锁图标显示 `3`（对应字符 `3ghi`）
6. 按 **C4** 键 → 选择键 3 的第 4 个候选，得到 `i`

### 2.2 锁定导航模式

按下 `Shift + BS` 可切换至锁定导航模式。锁定导航时锁图标显示 `L`。进入锁定导航模式后，需要再次使用`Shift + BS`退出。

锁定模式下，各键变为导航功能：

| 按键 | 功能 |
|------|------|
| `1` | 返回菜单页 |
| `2` | 光标上移 |
| `3` | 进入设置页 |
| `C1` | 光标移至行首（Home） |
| `4` | 光标左移 |
| `5` | **确认/进入**（选择菜单项、进入设置详情） |
| `6` | 光标右移 |
| `C2` | 光标移至行尾（End） |
| `7` | **返回**（从聊天/设置详情返回上级） |
| `8` | 光标下移 |
| `9` | 向前删除（Delete） |
| `C3` | 向上翻页（Page Up） |
| `C4` | 向下翻页（Page Down） |

**典型操作流程**：

1. 开机后在菜单页 → 按 `Shift + BS` 进入锁定模式
2. 按 `2`/`8` 上下选择联系人
3. 按 `5` 确认进入聊天页
4. 再按 `Shift + BS` 退出锁定模式，进入输入模式
5. 输入消息，按 `Shift + C4`（SEND）发送
6. 按 `Shift + BS` 进入锁定模式，按 `1` 或 `7` 返回菜单

---

## 3. 界面详解

### 3.1 状态栏

状态栏位于屏幕最顶部（15px 高），从左到右显示：

| 位置 | 内容 | 说明 |
|------|------|------|
| 左侧 | 时间 `MM/DD HH:MM` | 未设置时间时显示 `Set Time First!` |
| 中间 | 自定义标题 | 默认 `Lumi-net`，最多 16 字符 |
| 右侧 | LoRa 状态指示灯 | 彩色小方块，见下表 |
| 右侧 | 心跳指示灯 | 橙色小方块，仅在心跳**关闭**时显示 |
| 最右 | 电池百分比 | 如 `85%` |

**LoRa 状态指示灯颜色含义**：

| 颜色 | 状态 | 含义 |
|------|------|------|
| 🟢 绿色 | IDLE | 空闲，可收发 |
| 🔵 蓝色 | RECEIVING | 正在接收数据 |
| 🟡 黄色 | SENDING | 正在发送数据 |
| 🟣 紫色 | WAITING_ACK | 已发送，等待对方确认 |
| 🔴 红色 | TIMEOUT | 确认超时，发送失败 |

### 3.2 菜单页（联系人列表）

菜单页是设备的默认首页，显示所有已添加的联系人。

每个联系人显示两行信息：

```
> Alice         0x01          ← 第一行：别名 + 十六进制 ID
  02/02 15:37   02/02 14:28   ← 第二行：在线时间(左) + 消息时间(右)
```

- **光标 `>`** 指示当前选中的联系人
- 每页最多显示 5 个联系人，超出部分可通过翻页查看
- 排序方式：按 ID 从小到大
- 内置联系人 `ALL`（ID `0x00`）始终在列表最前面
- **在线时间**（左侧 `MM/DD HH:MM`）：该联系人最后一次被听到的时间（收到心跳或普通消息均会更新）。如果从未收到过任何通信，显示 `Maybe Offline`。
- **消息时间**（右侧 `MM/DD HH:MM`）：最后一次与该联系人交换消息的时间（发送或接收均会更新）。如果没有消息记录，显示 `No messages`。

### 3.3 聊天页

进入某个联系人的聊天页后，顶部显示标题：

```
CHAT | Alice (0x01)
```

消息列表格式：

```
02/02                     ← 日期分隔线（仅在日期变化时出现）
14:28 Alice: Hello!       ← [时间] [别名]: [消息内容]
14:29 Bob: How are you?   ← 对方消息
14:30 Alice: I'm fine.    ← 我的消息
```

- 消息内容自动换行
- 新消息自动追加到底部并滚动
- 向上滚动可加载更早的消息（每次加载 10 条）

### 3.4 输入区域

屏幕底部的输入区域包含：

```
[S][?]  Please Input...
   112
```

- **Shift 图标 `[S]`**：实心背景表示大写模式已开启，透明背景表示普通模式
- **Lock 图标 `[?]`**：显示 `L` 表示锁定模式，显示数字表示正在选键，显示 `?` 表示普通模式
- **输入框**：显示输入的文本，支持多行
- **剩余字符数**：左下角数字，最大 120 字符

---

## 4. 联系人管理

### 4.1 添加联系人

1. 进入**设置页**（按 `SETTINGS`）
2. 选择 **Add/Remove a contact**，按 `CONFIRM` 确认
3. 输入目标设备的十六进制 ID（如 `02`），按确认
4. 输入该联系人的别名（如 `Bob`），按确认
5. 联系人添加成功，自动返回设置页

**注意事项**：
- ID `0x00`（ALL）和 `0xFF` 为保留值，不允许添加
- 如果输入的 ID 已存在，会进入删除确认流程（见 4.2 节）

### 4.2 删除联系人

1. 在 **Add/Remove a contact** 中输入要删除的联系人 ID
2. 如果该 ID 已存在，设备显示确认提示：

   ```
   Delete contact 0x02 [Bob]?
   Type "0x02Bob" to confirm delete.
   ```

3. 输入 `0x` + ID + 别名（如 `0x02Bob`）进行确认
4. 输入错误则取消删除

**注意**：内置联系人 `ALL`（ID `0x00`）无法删除。

### 4.3 重命名联系人

1. 进入**设置页**
2. 选择 **Change Alias**
3. 输入要修改的联系人 ID，按确认
4. 输入新的别名，按确认
5. 别名更新成功，立即生效

### 4.4 联系人 ID 说明

- 每台设备的 ID 在固件中通过 `self_id` 变量定义（硬编码在 `uart.c` 中）
- 默认 ID 为 `0x01`
- 修改 ID 需要重新编译固件
- 所有设备的 ID 必须互不相同
- 只有添加了别名的联系人才会出现在菜单中；未添加别名的 ID 不会显示
- 收到来自未知 ID 的消息时，消息写入 Flash 但不更新菜单；后续手动添加该 ID 为联系人后，聊天页会从 Flash 加载历史消息

---

## 5. 发送与接收消息

### 5.1 直发消息

直发消息是点对点通信，需要对方设备自动确认收到（ACK）。

**发送流程**：
1. 进入目标联系人的聊天页
2. 输入消息（最多 120 字符）
3. 按 **Shift + C4**（SEND）发送
4. 状态栏指示灯变为**紫色**（等待 ACK）
5. 3 秒内收到 ACK → 消息保存成功，指示灯恢复**绿色**
6. 3 秒内未收到 ACK → 自动重试，最多重试 5 次
7. 5 次均超时 → 显示错误提示 `Message failed to send!`

**注意事项**：
- 发送期间（等待 ACK 时）无法发送新消息
- 对方设备需要在线且在通信范围内，否则会发送失败。
- 即使对方设备未将你添加为联系人，消息仍然会被接收，但不会自动出现在菜单中（无法看到）。如果之后对方手动添加你的 ID 为联系人，打开聊天页时会从 Flash 加载历史消息并显示。

### 5.2 广播消息（ALL）

广播消息发送给所有设备，无需确认ACK。

1. 在菜单页选择 **ALL**（ID `0x00`）进入聊天页
2. 输入消息并发送
3. 消息立即保存到本地 Flash，无需等待 ACK
4. 通信范围内所有设备均可收到

**广播消息特点**：
- 不需要对方确认
- 不保证送达
- 消息同时保存在本地和对方设备（若能收到）

### 5.3 消息去重

设备通过序列号（seq）机制自动去除重复消息。如果同一条消息因重试等原因被多次接收，只有第一条会被处理，后续重复消息被静默丢弃。

### 5.4 聊天记录

- 所有收发的消息都保存在外部 SPI Flash（约 8MB）
- 采用 A/B 块磨损均衡机制，延长 Flash 寿命
- 开机时自动扫描恢复写入位置
- 支持跨 A/B 块的连续读取
- 可在设置中清空聊天记录（需输入确认码）

---

## 6. 设置

### 6.1 设置项总览

| # | 设置项 | 功能简述 |
|---|--------|----------|
| 0 | Change Alias | 修改联系人显示名称 |
| 1 | Change User Color | 修改联系人消息颜色 |
| 2 | Change Interface Color | 修改界面主题颜色（6 个可配置项） |
| 3 | Wi-Fi (For Time) | 配置 Wi-Fi 用于时间同步 |
| 4 | Time | 手动设置时间或通过 Wi-Fi NTP 同步 |
| 5 | Auto Sleep Time | 设置自动休眠超时时间（秒） |
| 6 | Change Brightness | 调节屏幕亮度（10-100） |
| 7 | Customize Title | 自定义状态栏标题（最多 16 字符） |
| 8 | Add/Remove a contact | 添加或删除联系人 |
| 9 | Clear Chat History | 清空外部 Flash 中的聊天记录 |
| 10 | Clear NVS Data | 恢复出厂设置（清除所有设置，重启） |
| 11 | Heartbeat Enable | 开启/关闭心跳广播 |

### 6.2 修改联系人别名

1. 设置页 → **Change Alias** → 确认
2. 输入目标联系人的十六进制 ID（如 `01`）→ 确认
3. 输入新的别名（最多 16 字符）→ 确认
4. 别名立即生效

**注意**：如果输入的 ID 不存在，会提示 `Invalid ID!`。

### 6.3 修改用户颜色

为每个联系人设置独特的消息显示颜色。

1. 设置页 → **Change User Color** → 确认
2. 输入目标联系人的十六进制 ID → 确认
3. 输入 6 位十六进制颜色值（如 `00FF00` 表示绿色）→ 确认

颜色值格式：`RRGGBB`（红绿蓝各 2 位十六进制）。

### 6.4 修改界面颜色

界面有 6 个可配置颜色项：

| 索引 | 名称 | 说明 |
|------|------|------|
| 0 | COLOR_BACKGROUND | 页面背景色 |
| 1 | COLOR_MSG_TEXT | 消息文本颜色 |
| 2 | COLOR_MSG_TIME | 消息时间标签颜色 |
| 3 | COLOR_MSG_DATE | 日期分隔线颜色 |
| 4 | COLOR_STATUS_BAR | 状态栏背景色 |
| 5 | COLOR_TEXTAREA_BACKGROUND | 输入框背景色 |

操作步骤：
1. 设置页 → **Change Interface Color** → 确认
2. 输入颜色索引（0-5）→ 确认
3. 输入 6 位十六进制颜色值 → 确认

### 6.5 Wi-Fi 配置

Wi-Fi 仅用于时间同步（NTP），不用于消息传输。

**方式一：手动输入 SSID 和密码**

1. 设置页 → **Wi-Fi (For Time)** → 确认
2. 输入 Wi-Fi 名称（SSID，最多 32 字符）→ 确认
3. 输入 Wi-Fi 密码（至少 8 位）→ 确认
4. 设备自动连接 Wi-Fi，同步时间后关闭 Wi-Fi

**方式二：热点配网（Soft-AP）**

1. 设置页 → **Wi-Fi (For Time)** → 确认
2. **不输入任何内容**，直接按确认
3. 设备启动热点 `ESP32S3_Config`，屏幕显示随机密码
4. 用手机连接该热点，打开浏览器访问 `192.168.4.1`
5. 在网页中输入目标 Wi-Fi 的 SSID 和密码，提交
6. 设备自动切换到 STA 模式连接 Wi-Fi，同步时间后关闭

**注意事项**：
- 热点模式有 5 分钟超时，超时后自动关闭
- Wi-Fi 连接成功后会自动关闭以节省电量
- 时间同步使用 `ntp.aliyun.com`（主）和 `pool.ntp.org`（备）

### 6.6 时间设置

**手动设置**：
1. 设置页 → **Time** → 确认
2. 输入格式：`YYYYMMDDHHMMSS`（如 `20260202114514` 表示 2026 年 2 月 2 日 11:45:14）
3. 按确认，时间立即生效

**Wi-Fi NTP 同步**：
1. 设置页 → **Time** → 确认
2. **不输入任何内容**，直接按确认
3. 设备自动连接已保存的 Wi-Fi 并同步时间
4. 同步成功后显示 `Time Synced!`，Wi-Fi 自动关闭

时区固定为 UTC+8（中国标准时间）。

### 6.7 自动休眠时间

设置设备无操作后自动进入休眠的超时时间。

1. 设置页 → **Auto Sleep Time** → 确认
2. 输入超时秒数（如 `300` 表示 5 分钟）
3. 按确认

默认值为 300 秒（5 分钟）。设为 0 可禁用自动休眠。我们不建议禁用自动休眠，因为这将会非常耗电。

### 6.8 调节亮度

1. 设置页 → **Change Brightness** → 确认
2. 输入亮度值（10-100，如 `50`）
3. 按确认，亮度立即生效

亮度值 10 为最暗，100 为最亮。设置保存在 NVS 中，重启后仍然有效。

### 6.9 自定义状态栏标题

1. 设置页 → **Customize Title** → 确认
2. 输入新标题（最多 16 字符）
3. 按确认，标题立即更新

默认标题为 `Lumi-net`。

### 6.10 添加/删除联系人

详见 [第 4 章](#4-联系人管理)。

### 6.11 清空聊天记录

此操作会擦除整个外部 Flash 芯片（约 8MB），**所有聊天记录将永久丢失**。

1. 设置页 → **Clear Chat History** → 确认
2. 屏幕显示随机 4 位确认码（如 `A3F2`）
3. 输入 `CLEAR FLASH` + 空格 + 确认码（如 `CLEAR FLASH A3F2`）
4. 按确认，开始擦除（约 1 分钟）
5. 擦除完成后显示 `Flash cleared.`

**请谨慎操作！此操作不可逆。**

### 6.12 恢复出厂设置（清空 NVS）

此操作会清除所有 NVS 存储的设置（别名、颜色、亮度、Wi-Fi 配置等），并自动重启设备。

1. 设置页 → **Clear NVS Data** → 确认
2. 屏幕显示随机 4 位确认码
3. 输入 `CLEAR NVS` + 空格 + 确认码
4. 按确认，设备清除 NVS 后自动重启

**注意**：重启后所有设置恢复默认值，但聊天记录（存储在外部 Flash）不受影响。

### 6.13 心跳开关

心跳广播用于让其他设备知道你在线，默认开启，但关闭可以省电。

1. 设置页 → **Heartbeat Enable** → 确认
2. 屏幕显示当前状态（`ON` 或 `OFF`）
3. 按确认切换状态

- 开启时：设备每 3-4 分钟自动发送一次心跳广播
- 关闭时：状态栏显示橙色指示灯作为警告
- 心跳关闭后，其他设备将无法看到你的在线状态

---

## 7. 省电与休眠

### 7.1 自动休眠

设备在无操作超过设定时间（默认 300 秒）后自动进入睡眠模式。

进入休眠前的条件：
- 无待发送/待确认的消息
- Wi-Fi 处于空闲状态
- 无按键按下、无 LoRa 数据收发

休眠时：
- 屏幕背光关闭
- CPU 进入低功耗模式
- LoRa 模块进入休眠
- RTC 时钟继续运行

### 7.2 唤醒方式

| 唤醒源 | 触发条件 | 行为 |
|--------|----------|------|
| 键盘（EXT1） | 任意按键按下 | 亮屏 |
| LoRa AUX（EXT0） | AUX 引脚（GPIO21）变低 | 不亮屏，但保持唤醒 5 秒以处理消息 |
| 定时器 | 心跳发送时间 | 不亮屏，发送完心跳重新休眠 |

### 7.3 快速休眠机制

当设备被 LoRa AUX 或定时器唤醒时，会启动一个 5 秒的"快速休眠截止时间"：
- 在这 5 秒内收到新消息 → 重新计时 5 秒
- 5 秒内无活动 → 自动重新休眠
- 键盘唤醒不触发此机制，保持唤醒直到超时

---

## 8. 电池电量

ELC 支持 INA219 电流/电压传感器（I2C 地址 `0x40`），可监测电池电量。

- 电量每 30 秒更新一次
- 显示在状态栏最右侧（如 `85%`）
- 电压范围：3.30V（0%）至 4.20V（100%）
- 电量变化小于 1% 时不更新显示（防抖动）
- LoRa 发送或 Wi-Fi 活动期间跳过读取（避免电压跌落导致误判）

---

## 9. 界面颜色自定义

ELC 支持完全自定义界面颜色。所有颜色以 6 位十六进制值（`RRGGBB`）存储在 NVS 中。

**可配置的颜色项**：

| 索引 | NVS 键名 | 说明 | 默认值 |
|------|----------|------|--------|
| 0 | ic_0 | 背景色 | 深色 |
| 1 | ic_1 | 消息文本色 | 白色 |
| 2 | ic_2 | 消息时间色 | 灰色 |
| 3 | ic_3 | 日期分隔线色 | 灰色 |
| 4 | ic_4 | 状态栏背景色 | 深色 |
| 5 | ic_5 | 输入框背景色 | 深色 |

**每个联系人也有独立的颜色**（用户颜色），用于在聊天中区分不同人的消息。

修改方式详见 [6.3 节](#63-修改用户颜色) 和 [6.4 节](#64-修改界面颜色)。

---

## 附录 A：编译与烧录

### 环境搭建

请参考 [ESP-IDF 官方安装指南](https://docs.espressif.com/projects/esp-idf/en/latest/esp32s3/get-started/)。

推荐下载 `.zst` 离线安装包，避免在线安装的网络问题。

### 构建与烧录

```bash
# 设置目标芯片
idf.py set-target esp32s3

# 编译
idf.py build

# 烧录并打开串口监视器（替换 PORT 为实际串口号，如 COM7、/dev/ttyUSB0）
idf.py -p PORT flash monitor
```

### 关键配置

| 配置项 | 值 | 说明 |
|--------|-----|------|
| Flash Size | 16MB (QIO 80MHz) | 外部 Flash 容量 |
| 分区表 | `partitions_user.csv` | 自定义分区（factory app @ 0x10000, 4MB） |
| PSRAM | 启用 | LVGL 显示缓冲使用 PSRAM |

---

## 附录 B：硬件接线参考

### 主要外设接线

| 外设 | GPIO | 功能 | 说明 |
|------|------|------|------|
| **键盘行** | GPIO 5, 6, 7, 4 | 输出（开漏） | 矩阵键盘行线 |
| **键盘列** | GPIO 16, 17, 18, 8 | 输入（上拉） | 矩阵键盘列线，支持 EXT1 唤醒 |
| **LoRa UART TX** | GPIO 19 | UART1 TX | 连接 LoRa 模块 RX |
| **LoRa UART RX** | GPIO 20 | UART1 RX | 连接 LoRa 模块 TX |
| **LoRa MD0** | GPIO 47 | 输出 | 模式控制 0 |
| **LoRa MD1** | GPIO 48 | 输出 | 模式控制 1 |
| **LoRa AUX** | GPIO 21 | 输入（上拉） | 模块状态指示，支持 EXT0 唤醒 |
| **LCD SPI MOSI** | GPIO 11 | SPI2 MOSI | LCD 数据线 |
| **LCD SPI SCLK** | GPIO 12 | SPI2 SCLK | LCD 时钟线 |
| **LCD CS** | GPIO 10 | 输出 | LCD 片选 |
| **LCD DC** | GPIO 13 | 输出 | LCD 数据/命令选择 |
| **LCD Reset** | GPIO 14 | 输出 | LCD 复位 |
| **LCD 背光** | GPIO 9 | LEDC PWM | 亮度控制 |
| **Flash MISO** | GPIO 41 | SPI3 MISO | 外部 Flash 数据输入 |
| **Flash MOSI** | GPIO 38 | SPI3 MOSI | 外部 Flash 数据输出 |
| **Flash CLK** | GPIO 39 | SPI3 CLK | 外部 Flash 时钟 |
| **Flash CS** | GPIO 40 | 输出 | 外部 Flash 片选 |
| **I2C SDA** | GPIO 42 | I2C 数据 | INA219 电池传感器 |
| **I2C SCL** | GPIO 2 | I2C 时钟 | INA219 电池传感器 |
| **INA219 电源** | GPIO 1 | 输出 | 传感器供电控制 |

### LoRa 模块引脚

| 模块引脚 | 连接目标 |
|----------|----------|
| VCC | 3.3V |
| GND | GND |
| TXD | GPIO 20（UART1 RX） |
| RXD | GPIO 19（UART1 TX） |
| MD0 | GPIO 47 |
| MD1 | GPIO 48 |
| AUX | GPIO 21 |

### 注意事项

1. **电压**：ESP32-S3 GPIO 为 3.3V 逻辑，不可直接连接 5V 设备
2. **SPI 总线**：LCD 使用 SPI2 总线，外部 Flash 使用 SPI3 总线
3. **LoRa 模块配置**：模块需预先配置为定点透传模式、信道3C（470MHz）、本地ID（应与ESP32-S3相同）、唤醒时长
4. **键盘矩阵**：行线为开漏输出，列线上拉，按键按下时列线被拉低
5. **AUX 引脚**：LoRa 模块空闲时为高电平，有数据时变低，用于唤醒和状态检测

---

## 附录 C：sim_lora.py 模拟器使用

`sim_lora.py` 是一个 Python 脚本，通过 USB-UART 连接 LoRa 模块，模拟第二台 ELC 设备，用于测试通信功能。

### 安装依赖

```bash
pip install pyserial
```

### 基本用法

```bash
# 使用默认设置（self_id=0x05）连接 COM5
python sim_lora.py COM5

# 指定 self_id 为 0x03
python sim_lora.py COM5 --self-id 0x03

# 列出可用串口
python sim_lora.py --list-ports
```

### 交互命令

| 命令 | 说明 |
|------|------|
| `<文本>` | 发送消息给当前目标 |
| `/to <hex>` | 切换发送目标。`01`-`FE` 为定点发送（需对方 ACK），`00` 或 `FF` 为广播（无需 ACK） |
| `/hb` | 手动发送一次心跳（广播至 `0xFF`） |
| `/stats` | 显示收发统计（TX/RX/ACK 计数） |
| `/help` | 显示帮助 |
| `/q` | 退出 |

### 示例：测试完整通信流程

```bash
# 终端 1：启动模拟器，ID 为 0x05
python sim_lora.py COM5 --self-id 0x05

# 终端 2：与 ELC 设备（ID 0x01）对话
# 在模拟器中：
> /to 01              # 切换目标到 ELC 设备
> Hello from PC!      # 发送消息
# ELC 设备应收到消息并自动回复 ACK

# 在 ELC 设备上：
# 进入 ALL 聊天页，发送广播消息
# 模拟器应收到广播
```

### 注意事项

1. **LoRa 模块配置**：模块需预先配置为定点透传模式
2. **波特率**：默认 9600bps，8N1 格式，与固件一致（可通过 `--baud` 参数修改）
3. **自动 ACK**：收到发给本机的单播消息时自动回复 ACK；收到广播消息（目标 `0x00` 或 `0xFF`）时不回复
4. **自动心跳**：后台每 180-240 秒（随机间隔）自动发送一次心跳广播，也可通过 `/hb` 手动触发
5. **帧格式**：与固件完全一致，完整线上包结构如下：

```
[routing 3B] + [AA 55] + [seq] + [self_id] + [target_id] + [timestamp 4B] + [data_len] + [data 0-120B] + [CRC8]
```

- **routing**：3 字节路由头（`[group, target, channel]`，广播时为 `[0xFF, 0xFF, 0x3C]`）
- **AA 55**：帧头标识
- **seq**：序列号（用于消息去重）
- **self_id / target_id**：发送方 / 接收方 ID
- **timestamp**：4 字节时间戳（月,日,时,分）
- **data_len**：数据长度（0-120）
- **CRC8**：校验码（多项式 x^8+x^5+x^4+1，初始值 0）

---

*本手册适用于 ELC (ESP32-S3 LoRa Chat) 固件版本。如有更新，请以最新版本为准。*
